# After Hours is an adults-only party game. The landing page is indexable so the
# product can be found; the play surface, which renders explicit prompts and
# carries player names in its query string, is not.
User-agent: *
Allow: /$
Allow: /terms
Allow: /privacy
Disallow: /game
