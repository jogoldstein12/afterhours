# After Hours — Holistic App Audit

**Date:** August 26, 2026 · **Last updated:** August 26, 2026 (post-remediation)
**Scope:** Functionality, UX, UI, content deck, code quality, infrastructure.
**Context:** Internal development phase, used by a small group of friends. Security explicitly out of scope for this audit.
**Method:** Full source read, production build + typecheck + lint run, live app booted and exercised with Playwright (mobile + desktop viewports), full 890-prompt deck review, standalone `game.html` diffed against the deck.

> **Status legend used throughout:** ✅ Fixed · ⚠️ Partially addressed · ⬜ Open (not yet started) · 🚫 Won't do / out of scope for now.
> A dated **Resolution log** follows the scorecard; individual findings below are tagged inline with their current status.

---

## Verdict

The app is in better shape than most first-audit projects: the visual identity is coherent and confident, the core pass-the-phone loop works, the standalone `game.html` is genuinely offline-capable and byte-identical in content to the main deck, and the production build passes. The biggest problems are not in what was built but in what was turned off, duplicated, or left half-connected: every quality gate is disabled, the PWA layer is broken in ways that 404 on every page load, the game logic exists twice and has already drifted, and the prompt deck — the actual product — has heavy duplication, inverted difficulty, and a handful of prompts with real consent problems.

**Scorecard** (original grade → current grade after remediation)

| Area | Then | Now | One-liner |
|---|---|---|---|
| Visual design / UI | B+ | B+ | Coherent neon identity; a few mobile layout and redundancy issues remain |
| Core gameplay | B− | A− | Deck-reset bug, unfair turn order, and lost-progress mis-taps all fixed; fair rotation, undo, timer, wake lock added |
| Content deck | C | B− | Drink economy capped and rebalanced, consent outliers removed, typos fixed; duplicate clusters and tier miscalibration still open |
| Code quality | C+ | B+ | Gates on, dead deps and 22 unused components removed, engine behavior reconciled; code still duplicated across two engines |
| PWA / offline | D | ✅ n/a | PWA layer removed (Option B); `game.html` is the verified offline story |
| Infra / tooling | D+ | B | CI added (typecheck + lint + build + deck freshness); monitoring/analytics still absent |

---

## Resolution log — August 26, 2026

The bulk of the audit's red flags and bug list were remediated the same day, merged to `main` via PR #4 (plus follow-ups). Summary of what shipped:

**Red flags**
- ✅ **1.1 Quality gates** — `ignoreBuildErrors`/`ignoreDuringBuilds` removed, ESLint configured for the first time (passes clean), GitHub Actions CI runs typecheck + lint + build + a `game.html`/`prompts.csv` freshness check.
- ✅ **1.2 PWA layer** — removed entirely (Option B). Broken `/register-sw.js`, unmaintained `next-pwa`, and `offline.html` gone; manifest kept for home-screen install with correct dark colors and real icons. `game.html` is the offline story.
- ✅ **1.3 Mid-game deck reset** — roster changes no longer reload the deck (deck reloads only on level change).
- ✅ **1.4 Fonts loaded twice** — manual Google Fonts `<link>` removed; `next/font` only.
- ⚠️ **1.5 Consent & calibration** — prompt 187 (forced groping) and 243 (STI disclosure) removed, 182 reworked into a vote; **drink economy capped at 3 across all tiers**, fixing the Medium-harsher-than-Extreme inversion. A Skip button was added then removed at the owner's request. Still open: tier misfiling and the NHIE-includes-Extreme signal (needs content decisions).
- ⬜ **1.6 Deck duplicates** — the 60+ duplicate clusters still need a content-judgment dedupe pass.
- ⚠️ **1.7 Engine drift** — the app and `game.html` now behave identically (deck reset, level re-tap, remove-player, rotation, undo, timer all matched and test-covered). The *code* is still duplicated across two implementations — the shared-engine refactor remains open.
- ✅ **1.8 Docs that lie** — moderation feature removed; README no longer advertises moderation or the nonexistent gender feature; `docs/prompts.csv` is now regenerated from the deck by `build:html` (CI-enforced) so it can't go stale.

**Functionality bugs** — ✅ 2.1 comma names, ✅ 2.2 unvalidated level, ✅ 2.3 remove-player index, ✅ 2.4 unfair turn order (shuffled rounds), ✅ 2.5 prefix misfires, ✅ 2.6 unbounded drink counts (capped at 3), ✅ 2.8 moderation null-crash (feature removed), ✅ 2.9 standalone level re-tap, ✅ 2.10 standalone remove-player, ✅ 2.11 toast timing, ✅ deck typos. Partial: ⚠️ 2.7 persistence (last group remembered via localStorage; mid-game resume still open). Open: ⬜ 2.12 "Save Changes" applies instantly.

**Gameplay added** — ✅ fair shuffled-round rotation, ✅ undo last card, ✅ countdown timer on timed prompts, ✅ screen wake lock, ✅ remembered group + level.

**UX/UI** — ✅ players remembered (no retyping), ✅ accidental-Next recoverable (undo), ✅ screen no longer sleeps, ✅ `aria-live` prompt announcements, ✅ `prefers-reduced-motion`, ✅ white-splash fix, ✅ `robots.txt`. Open: ⬜ level descriptions, ⬜ NHIE intensity signal, ⬜ disabled-until-valid Start, ⬜ mobile header polish, ⬜ End-Game dialog vocabulary, ⬜ end-of-game recap, ⬜ low-contrast text, ⬜ drink counters.

**Code / infra** — ✅ dead deps removed (firebase, patch-package, recharts, date-fns, form stack, 13 radix pkgs), ✅ 22 of 33 unused shadcn components deleted, ✅ `game.html` freshness enforced in CI, ✅ README clone URL. Open: ⬜ in-place state mutation / `key={index}`, ⬜ dead `Header` nav placeholder, ⬜ `.text-secondary` collision, ⬜ error monitoring + analytics, ⬜ `CLAUDE.md`. Resolved-by-removal: ✅ `.env.example` (no key needed now).

**Still the top open items** (need product/content judgment, deferred by design): deck dedupe (1.6), tier re-calibration and NHIE signal (1.5), the shared game-engine refactor (1.7), and the drink-counter + end-game recap feature.

---

## 1. Red flags

These are the things I'd want fixed or decided before inviting anyone beyond the friend group.

### 1.1 Every quality gate is off — ✅ Fixed
`next.config.ts` sets `typescript.ignoreBuildErrors: true` and `eslint.ignoreDuringBuilds: true`. ESLint has **never been configured** — `npm run lint` drops into the first-run setup wizard. There are zero tests and no CI. The build output literally prints "Skipping validation of types / Skipping linting."

The good news: turning the gates back on is nearly free right now. The only current typecheck error is a missing type declaration for `next-pwa` (a one-line `declare module 'next-pwa'` shim fixes it). This is the cheapest moment the project will ever have to flip these on; every week of development with gates off raises the price.

### 1.2 The PWA layer is broken and misleading — ✅ Fixed (removed, Option B)
- `src/app/layout.tsx:38` loads `/register-sw.js`, **which does not exist** — a 404 fires on every page load of every route (confirmed live). `next-pwa` with `register: true` injects its own registration anyway, so the tag is at best dead weight and at worst a sign the SW setup was never verified.
- `public/manifest.json` sets `theme_color` and `background_color` to `#ffffff` — a white splash/status chrome on a near-black neon app.
- The manifest points both icon entries (192 and 512) at the same `After_Hours_App_Icon.png`, while the purpose-built `icon-192x192.png` / `icon-512x512.png` sit unused next to it. No maskable icon.
- `next.config.ts` has a `runtimeCaching` rule for `/api/prompts` — an endpoint that doesn't exist.
- `next-pwa@5.6` is unmaintained (last release 2022) and predates Next 15 / the app router. If offline PWA is a real requirement, migrate to a maintained equivalent (e.g. `@serwist/next`) and actually test install + airplane mode. If it isn't — the standalone `game.html` already covers offline — consider deleting the PWA layer entirely rather than shipping a broken one.

### 1.3 Adding or removing a player mid-game silently resets the deck — ✅ Fixed
`src/app/game/page.tsx:102-107` — the effect that reloads prompts depends on `players.length`, so any roster change through Manage Group wipes `usedPromptIds` and deals a fresh deck. An hour into a game, adding a latecomer erases all progress with no warning. (The standalone `game.html` does *not* do this — one of several behavioral drifts between the two implementations.)

### 1.4 Fonts are loaded twice, two different ways — ✅ Fixed
`layout.tsx` self-hosts Inter and Space Grotesk via `next/font` **and** loads the same two families again from a manual Google Fonts `<link>`. Double download, external dependency that defeats offline, and a layout-shift risk. Delete the manual `<link>` block (lines 32-34); `next/font` alone is strictly better.

### 1.5 Consent and calibration problems in the deck — ⚠️ Partially addressed
Security may be out of scope, but content safety is the product's actual risk surface for a party game:

- **id 187** — "biggest boobs must allow everyone to feel them" — mandates group physical contact with no opt-out. This is the worst offender and should be removed or rewritten with a drink-out alternative.
- Several forced-contact prompts (182, 199, 200, and the Extreme touch/strip clusters) have no escape hatch because **the app has no Skip button** — the roadmap doc already flags this as the top "do first" item and it's still missing. A skip-with-penalty is the single mechanic that makes the entire Extreme tier safe to play.
- **id 243** asks for STI disclosure — sensitive health information as a party forfeit.
- Tier misfiling: a large batch of hard-Extreme content sits in Medium (anal 141, orgy 152, public masturbation 163, groping 187, choking 263, stripping 210-215, showing nudes 189/224), while some Extreme entries are tame (306 "most embarrassing thing on your phone"). Duplicate prompts even carry contradictory tiers: 161 (Medium) and 322 (Extreme) are the same "sex outdoors" prompt.
- **The drink economy is inverted.** Medium hands out 8-12 drink penalties; Extreme never exceeds 5 and has essentially zero give-out mechanics. Medium is objectively the drunkest mode.
- **NHIE mode mixes all tiers with no filter** — 80 of its 125 prompts are Extreme. A group choosing "Never Have I Ever" expecting something tamer than Extreme gets Extreme content immediately, with no signal on the setup screen.

### 1.6 The deck is one-third duplicates — ⬜ Open (needs content judgment)
890 prompts, but the content audit found **60+ duplicate or near-duplicate clusters** — some with five to eight variants of the same idea (body count ×6, lap dance ×8, body shots ×7, "sex in a car" ×3, "faked an orgasm" ×5, private-room ×6...). Effective unique content is far below the headline number, and players will notice repeats within a single long session since duplicates aren't linked (using one doesn't retire its twins).

### 1.7 The game engine exists twice and has already drifted — ⚠️ Behavior reconciled; code still duplicated
The Next.js app and `tools/standalone-template.html` each implement the full game logic independently. They already disagree on: deck-reset on roster change (1.3 above), what happens when you re-tap the current level (standalone silently resets the deck — a real bug), remove-player index semantics, and used-prompt tracking keys. Every gameplay change now has to be made twice and tested twice, and nothing checks they stay in sync. `tools/build-standalone-html.js` also only works by accident: its slice captures `[] = [...]` (a destructuring expression) rather than the array literal, and it has latent `String.replace('$…')` and `</script>`-injection hazards. Worth a small refactor: emit the deck as JSON and share one game-logic module between both targets.

### 1.8 Documentation and data that lie — ✅ Fixed
- `docs/prompts.csv` claims to be the deck spreadsheet but is stale: 613 rows vs 890 prompts, missing the entire 900-1176 batch, and contains duplicated ids 246-259 from a pre-renumbering era. Regenerate it from `prompts.ts` in the build script, or delete it.
- `README.md` advertises "Player & Gender-Specific Prompts: the game supports prompts that can be targeted based on player gender" — **no such feature exists.** Meanwhile 6 prompts (186, 187, 222, 240, 241, 242) actually do depend on gender the app never collects.
- The AI moderation feature (Genkit flow + `/moderator-test` page) is disconnected from gameplay — nothing feeds it, its output goes nowhere, the required Gemini API key is undocumented (no `.env.example`), and the internal test page ships in the production bundle. Decide: wire it into a real custom-prompts feature, or cut it and drop the genkit dependencies (which also emit handlebars warnings in every build).

---

## 2. Functionality bugs

| # | Bug | Status |
|---|---|---|
| 2.1 | Player names containing a comma break the game — names are comma-joined into the URL and split on `','` | ✅ Fixed (repeated `?player=` params) |
| 2.2 | `nsfwLevel` query param is cast, never validated — deals an empty deck and shows "Last Call!" immediately | ✅ Fixed (validated against known modes) |
| 2.3 | `handleRemovePlayer` index math is wrong: stale `players.length`, and decrements even when the current player is removed | ✅ Fixed |
| 2.4 | Next player is uniform-random — the same person can be picked 3-4 turns in a row while someone else never goes | ✅ Fixed (shuffled-round rotation) |
| 2.5 | Name-prefix heuristic misfires: id 238 "Josh, Youngest person…", ids 223/224/226, and `{{randomOtherPlayer}}`-opening double-address | ✅ Fixed (both engines) |
| 2.6 | Unbounded drink counts: id 124, id 214 (2 per shirt button ≈ 16+), ids 836/986/1003/1091 (drink-your-number) | ✅ Fixed (capped at 3, incl. multipliers) |
| 2.7 | No state persistence — closing the tab mid-game loses everything | ⚠️ Partial — last group + level remembered; mid-game resume still open |
| 2.8 | `moderateContentFlow` returns `output!` — crashes on a null model response | ✅ Resolved (moderation feature removed) |
| 2.9 | Standalone: tapping the already-selected level silently resets the deck; label click double-fires | ✅ Fixed |
| 2.10 | Standalone: removing an earlier player shifts "current player" and re-randomizes the visible card | ✅ Fixed (rotation rework) |
| 2.11 | Toasts: `TOAST_LIMIT = 1` and `TOAST_REMOVE_DELAY = 1000000` (~16 min) | ✅ Fixed (5s dismiss) |
| 2.12 | "Save Changes" button implies staged edits, but every change applies instantly (both versions) | ⬜ Open |

Typos/grammar in the deck (worst offenders): 112, 180, 281, 226, 204, 240/242, 352 — ✅ **Fixed** (18 typo/grammar fixes plus punctuation on the 271-284 block).

---

## 3. UX findings

**Setup screen**
- ⬜ Levels are chosen blind — no description of what Mild/Medium/Extreme mean, and the difference is dramatic.
- ⬜ **NHIE gives no hint that it spans all spice levels** including Extreme. This needs a label or, better, a spice sub-filter.
- ⬜ Start button is always enabled and error-toasts on failure instead of being disabled with a hint.
- ✅ ~~Players re-type names every single session~~ — **Fixed:** last group + level are remembered in localStorage and prefilled.

**Game screen**
- ⬜ Mobile header is cramped: the logo is squeezed between the player name and the count, the wordmark wraps to two lines, and a long player name will collide with it. The absolutely-positioned `scale-[0.6]` logo hack is the cause.
- ⬜ "# OF PLAYERS" badge above "3 PLAYERS" text says the same thing twice; that header slot could show something useful (round number, prompts remaining).
- ✅ ~~An accidental tap on "Next Player" is unrecoverable~~ — **Fixed:** an Undo button restores the exact card, player, and turn queue.
- ⚠️ No skip (Skip was added then removed at owner's request), ⬜ no drink tally, ✅ **timer added** for the "for 30 seconds" prompts.
- ✅ ~~The screen sleeps mid-game~~ — **Fixed:** the game holds a `navigator.wakeLock` and re-acquires it on visibility change.
- ⬜ "End Game" opens a dialog titled "Start a New Session?" with a red "New Game" button — vocabulary mismatch ("End" vs "Start" vs "New").
- ⬜ Deck exhaustion ("Last Call!") is abrupt — no stats, no recap, no celebration.

**Accessibility**
- ✅ ~~New prompts are not announced~~ — **Fixed:** the card is `aria-live="polite"` (both engines).
- ⬜ Multiple text elements at 50-60% white opacity on near-black are below WCAG AA contrast.
- ✅ ~~Continuous smoke/neon animations have no `prefers-reduced-motion` fallback~~ — **Fixed** (both engines).

---

## 4. UI / visual

The neon identity is genuinely good — consistent palette, committed dark theme, the mode-colored card borders (violet/pink/red) are a nice touch of information-in-form. Issues:

- ✅ ~~White flash risk on load from white manifest colors~~ — **Fixed:** manifest now uses the dark palette (and the PWA chrome itself is gone).
- ⬜ `globals.css` defines `.text-secondary`, overriding Tailwind's own utility of the same name — a collision waiting to confuse someone.
- ⬜ `@apply dark` inside `body` styles is a no-op hack; dark mode is already forced via the `html` class. The duplicated `:root` / `.dark` token blocks are pure copy-paste weight.
- ⬜ The dead `Header` nav placeholder (`{/* ... existing navigation buttons ... */}`) ships in the component.

---

## 5. Code quality & repo hygiene

- ✅ ~~Dead dependencies~~ — **Fixed:** `firebase`, `patch-package`, `recharts`, `react-day-picker`, `date-fns`, the `react-hook-form`/`zod` stack, and 13 unused radix packages removed.
- ✅ ~~24 of 33 shadcn components are unused~~ — **Fixed:** 22 unreachable components deleted (verified transitively); 11 in-use components kept.
- ⬜ `page.tsx` mutates state objects in place (`newPlayers[index].name = name`) and player rows use `key={index}` — both work today, both are the classic source of future subtle bugs.
- ⬜ `game/page.tsx` sets a "Game Over!" message that is never rendered (the "Last Call!" block renders instead).
- ✅ ~~`game.html` staleness relies on convention~~ — **Fixed:** CI now runs `build:html` and fails on any `game.html`/`prompts.csv` drift.
- ⚠️ No `CLAUDE.md`/CONTRIBUTING (⬜ still open); the README's install `git clone` URL is ✅ fixed.

---

## 6. Infrastructure

- **Hosting:** Firebase App Hosting (`apphosting.yaml`, `maxInstances: 1`) — fine for the friends phase. The app is now **fully static** (moderation removed), so it could move to `output: 'export'` on any static host if desired. Not deployed on Vercel or Railway (checked both).
- ✅ ~~No CI whatsoever~~ — **Fixed:** GitHub Actions runs typecheck + lint + build + `game.html`/`prompts.csv` freshness on every PR and push to `main`.
- ⬜ **No error monitoring and no analytics** — still no way to know if the app crashes at a party or which levels people actually play.
- ✅ ~~No `.env.example`~~ — **Resolved:** the Gemini-key requirement is gone with the moderation feature.
- ✅ ~~`next-pwa` is the one rotten dependency~~ — **Fixed:** removed. Toolchain pins otherwise healthy (Node 20, Next 15.3.8, React 18, lockfile committed).

---

## 7. What's working well

Credit where due — these should be preserved through any refactor:

- The standalone `game.html` is a genuinely great artifact: fully offline, zero dependencies, XSS-safe (all user text via `textContent`), graceful font fallback, and byte-identical prompt content to the main deck.
- The NHIE mode design — a regex-derived virtual tier rather than a fourth content silo — is clean and means NHIE prompts never fork from their home tiers.
- The visual identity is distinctive and consistently executed across both implementations.
- The existing `afterhours_ux_improvements.md` roadmap is honest and well-prioritized. Its "Do First" list is validated by this audit; most of it (typos, fair rotation, timer, player memory) has now shipped.
- `docs/blueprint.md` shows the original scope; the core of it (setup → randomizer → pass-the-phone → filter) is fully delivered.

---

## 8. Recommended order of work

**This week (hours, not days)** — ✅ **Done**
1. ✅ Removed the `/register-sw.js` tag and manual Google Fonts `<link>`; fixed manifest colors/icons (then removed the PWA layer entirely).
2. ✅ Flipped `ignoreBuildErrors`/`ignoreDuringBuilds` off, configured ESLint, added the GitHub Actions workflow (typecheck + lint + build + freshness).
3. ✅ Fixed the mid-game deck-reset effect; validated `nsfwLevel`; encoded players safely (repeated `?player=` params).
4. ✅ Removed/rewrote the consent-problem prompts (187, 243, 182). (Skip button was shipped then removed at owner's request.)
5. ⬜ **Still open (deferred):** level descriptions + an "includes all intensities" note on NHIE.

**Next (a weekend)** — mostly done
6. ⬜ **Deck cleanup pass still open** (needs content judgment): dedupe the 60+ clusters, re-tier the misfiled prompts. ✅ Drink economy already capped at 3 and rebalanced; ✅ typos fixed; ✅ `prompts.csv` auto-regenerated.
7. ✅ Player-name memory (localStorage) and fair rotation (shuffled rounds).
8. ✅ Wake lock and undo-last-card. ⬜ Drink counters still open.

**Strategic (decide, then do)**
9. ⬜ **Still open:** single-source the game engine so the app and standalone build share logic (behavior is reconciled and test-covered, but the code is still duplicated). ✅ `build-standalone-html.js` hardened (function-form `replace`).
10. ✅ **Done:** moderation feature removed (flow, test page, genkit/firebase deps).
11. ✅ **Done:** PWA dropped (Option B); `game.html` is the official offline story.

**Remaining open items (in priority order):** deck dedupe + re-tiering (§1.6, §1.5), NHIE intensity signal (§1.5), drink counters + end-game recap, mid-game state persistence (§2.7), shared game-engine refactor (§1.7), then the smaller UI/a11y cleanups (§4 collisions, contrast, dead `Header` nav) and error monitoring/analytics (§6).

---

*Appendix — full duplicate-cluster list, tier-outlier ids, and typo list available in the audit working notes; the ids cited above are the actionable core.*
